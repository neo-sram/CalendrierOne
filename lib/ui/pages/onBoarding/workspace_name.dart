import 'package:flutter/material.dart';

class WorkSpaceName extends StatelessWidget {
  const WorkSpaceName({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}